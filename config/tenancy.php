<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default clinic for legacy rows and unauthenticated creates (tests, CLI).
    |--------------------------------------------------------------------------
    */
    'default_clinic_id' => (int) env('DEFAULT_CLINIC_ID', 1),

];
