# ═══════════════════════════════════════════════════════════
#  MobaXterm — السيرفر 31.97.61.205
# ═══════════════════════════════════════════════════════════

# ── إذا طلع: Docker not installed ──
cd /var/www/clinic-system-main
git pull origin main
bash scripts/install-docker-vps.sh
bash scripts/server-deploy-all.sh

# ── تحديث الصفحة الرئيسية (تطبيق Android + زر حمّل APK) ──
cd /var/www/clinic-system-main
git pull origin main
bash scripts/fix-storage-permissions.sh

# ── أو أمر واحد (بعد git pull) ──
# bash scripts/server-deploy-all.sh
# (يثبّت Docker تلقائياً إذا ناقص)

# ── firebase-credentials.json ──
# cp storage/app/clinic-system-*-firebase-adminsdk-*.json storage/app/firebase-credentials.json
